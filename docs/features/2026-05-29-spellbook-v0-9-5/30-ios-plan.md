# V0.9.5 Spellbook Codex — iOS Replication Plan

> Inputs: [`00-design.md`](00-design.md), signed [`20-replication-trigger.md`](20-replication-trigger.md)

iOS replication must not start until `20-replication-trigger.md` has `replication_approved: true`.

Stage 4a is blocked until the replication trigger is signed. After that, this file will translate the frozen HarmonyOS delta into iOS tasks without redesigning the feature.
