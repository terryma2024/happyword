# Spec: V0.5 client follow-up (HarmonyOS)

**Branch:** `feat/v0.5-client-followup`

**Status:** Approved 2026-05-01.

**Server prerequisite:** `feat/v0.5.2-thru-v0.5.7` merged or running. Pack JSON schema versions 1 / 2 / 4 / 5 are all live.

---

## 1. Why this branch exists

After `feat/v0.5.2-thru-v0.5.7` shipped on the server, the HarmonyOS client started failing on cold start with **"Failed to load words"** in `BattlePage`. Reproduces against `https://happyword.vercel.app/api/v1/packs/latest.json`, which currently returns a 1-word pack:

```json
{"version":1,"schema_version":1,"published_at":"...","words":[
  {"id":"fruit-apple","word":"apple","meaningZh":"苹果","category":"fruit","difficulty":1}
]}
```

`WordPackBootstrapper.bootstrap` reads the cache (populated by a previous background refresh that pulled this 1-word pack), parses 1 entry, hands it to a `WordRepository`, which `BattlePage.aboutToAppear` then feeds to `QuestionGenerator`. `QuestionGenerator.nextQuestion` enforces `repo.size() >= 3` and throws — the surrounding `.catch` in BattlePage swallows the throw and sets `prompt = 'Failed to load words'`. The empty option buttons + working HP/combo/timer in the live screenshot match this state exactly.

Beyond the immediate bug, the client never consumed the v0.5 server schema bumps that `feat/v0.5.2-thru-v0.5.7` shipped:

- schema_v2 — `distractors[]` / `example.{en,zh}` per word
- schema_v4 — top-level `categories[]` with server-supplied `storyZh` / `labelZh`
- schema_v5 — per-word `illustrationUrl` / `audioUrl` (Vercel Blob)

This branch closes that gap and adds an in-app, parent-PIN-gated admin shell so an operator can drive the server APIs without curl.

---

## 2. Goals / non-goals

### Goals
1. **Stop the emulator from breaking** on the next cold start.
2. Consume schema bumps end-to-end (v2 / v4 / v5).
3. Lazy on-disk asset cache (illustrations + audio).
4. ETag-aware client-side cache record.
5. Parent-PIN-gated admin shell inside the HarmonyOS app, debug-build entry only.

### Non-goals
- New gameplay features. Question kinds, animations, coin rules unchanged.
- Server changes (server is feature-complete at V0.5.7).
- Release-build admin UI surface.
- iOS / Android / web ports of the admin shell.
- Push notifications.

---

## 3. Architecture — sub-version sequence

One TDD-disciplined commit per row.

| # | Layer | Files (NEW *italic*) | Atomic acceptance |
|---|---|---|---|
| **VC0.5.A** | Cache hygiene + min-size sanity guard | `services/WordPackCache.ets`, `services/WordPackBootstrapper.ets`, `test/WordPackBootstrapper.test.ets`, `test/WordPackCache.test.ets` | Cached pack with `< MIN_REPO_SIZE` valid words is dropped; bootstrap falls through to rawfile. Prefs key bumps to `pack_v2` so legacy bad cache is invalidated. |
| **VC0.5.B** | Cache record v2 (`{etag, body, schemaVersion, fetchedAt}`) + `If-None-Match` | `services/WordPackCache.ets`, `services/RemoteWordPackService.ets`, `services/WordPackBootstrapper.ets`, both tests | 200 response writes etag + body; 304 keeps both, just bumps `fetchedAt`; missing etag header keeps the existing graceful path. |
| **VC0.5.C** | `WordEntry` schema bumps (v2 fields) + `QuestionGenerator` server-distractor preference | `models/WordEntry.ets`, `services/WordRepository.ets`, `services/QuestionGenerator.ets`, `test/QuestionGenerator.test.ets` (new) | When entry.distractors length ≥ 2 and unique vs. answer, the generator uses them instead of sampling. Collisions / dupes filter out — falls back to old behaviour gracefully. |
| **VC0.5.D** | *`services/RemoteAssetCache.ets`* (lazy fetch, on-disk LRU 50 illustrations + 100 audios) + `components/CharacterCard.ets` / `pages/MonsterCodexPage.ets` / `services/PronunciationService.ets` consumers | + *`test/RemoteAssetCache.test.ets`* | Resolve(url) returns a local file URI or undefined. UI never blocks on a download. Bundled SVG / TTS fallback on miss / failure. |
| **VC0.5.E** | *`services/CategoryCatalog.ets`* + `pages/HomePage.ets` storyZh overlay | + *`test/CategoryCatalog.test.ets`* | When pack.categories[i].id matches an AdventureCatalog region id, `storyZh` / `labelZh` overlay the bundled blurb. Orphan server categories are ignored on this branch. |
| **VC0.5.F** | Admin shell inside the app | *`services/AdminApiClient.ets`*, *`pages/admin/AdminLoginPage.ets`*, *`pages/admin/AdminWordsPage.ets`*, *`pages/admin/AdminDraftsPage.ets`*, *`pages/admin/AdminLessonImportPage.ets`*, *`pages/admin/AdminAssetUploadPage.ets`*, *`pages/admin/AdminPacksPage.ets`*, parent-PIN gate reuse, debug-only HomePage entry, *`test/AdminApiClient.test.ets`* | Login → JWT in AppStorage; CRUD against `/api/v1/admin/*`. Release builds hide the entry. |

---

## 4. Data flow

```
Server V0.5.7 → /api/v1/packs/latest.json (schema_v 1/2/4/5)
                  ↓ ETag-aware fetch (B)
WordPackCache v2 record → WordPackBootstrapper (sanity guard, A)
                  ↓
WordRepository (+distractors/example/illustrationUrl/audioUrl, C)
                  ↓                          ↘
QuestionGenerator (server distractors first)   RemoteAssetCache (D)
                  ↓                                        ↓
            BattleEngine                       File URI / fallback
                  ↓
       BattlePage / SpellingArea / CharacterCard
```

---

## 5. Storage shapes

### 5.1 WordPackCache v2 record (preferences key `pack_v2`)
```jsonc
{
  "etag": "\"3\"",
  "body": "<full pack JSON string>",
  "schemaVersion": 5,
  "fetchedAt": "2026-05-01T08:42:00.000Z"
}
```

`looksValid` = JSON.parse succeeds AND `body` parses to an object with `words: any[]`. Empty etag is allowed (server may not send one).

### 5.2 RemoteAssetCache on-disk index
```
${context.filesDir}/asset-cache/
  ├─ illustrations/
  │   ├─ <sha256-prefix-12>.png
  │   └─ ...
  └─ audio/
      ├─ <sha256-prefix-12>.mp3
      └─ ...
```

Index is rebuilt at process start by listing the two directories; URL→localPath is a session-scoped `Map`. LRU eviction runs when the limits are exceeded after a successful new write — never mid-render.

---

## 6. Error handling (cumulative across layers)

| Failure | Fallback |
|---|---|
| Cache JSON parse fails | Rawfile loader. |
| Cached pack < 3 valid words | Drop cache, rawfile loader. |
| Background refresh 304 / network error | Keep current cache. |
| Asset URL download fails (timeout / 4xx / 5xx) | Bundled SVG / TTS pronunciation. Log once per URL per session. |
| Pack contains illustrationUrl / audioUrl but RemoteAssetCache hits LRU cap | Evict oldest then proceed; if still failing, fall back. |
| Admin API 401 / 403 | Toast + return to AdminLoginPage; clear stale JWT. |
| Admin API 5xx | Toast with code; stay on the current page so the operator can retry. |

---

## 7. Testing posture

- **Unit tests are the floor.** Every layer ships with new entry/src/test/*.test.ets cases that prove the behaviour change. Existing tests must keep passing.
- **No live network.** All HTTP calls go through `HttpRequester` (existing pattern in RemoteWordPackService); `AdminApiClient` mirrors that injection seam.
- **No live disk in unit tests.** `RemoteAssetCache` accepts an injectable filesystem-like adapter the same way `WordPackCache` accepts `StringPreferencesLike`.
- **UiTest smoke for admin pages.** Each admin page gets a render-and-bind test that asserts the page composes without throwing and a key element (login form, list row, upload button) is visible. Full e2e is deferred to V0.6.

---

## 8. Toolchain & build gates

- **No new dependencies.** Everything uses ohos.* / @kit.* / existing project packages.
- **Codelinter clean** after every commit (per project rule).
- **`hvigorw assembleHap` clean** after every commit.
- **Debug-vs-release admin entry** uses the same `BuildProfile.BUILD_MODE_NAME` toggle that `RemoteWordPackConfig.pickServerBaseUrl` already relies on. No new build profile keys.

---

## 9. Out of scope (tracked but deferred)

- Server-driven admin UX polish (V0.6).
- Lesson-import region card synthesis on HomePage (V0.6 admin UI prerequisite).
- Real-device push notifications.
- Asset cache size telemetry / analytics dashboard.

---

## 10. Self-review checklist

- ☑ No "TBD" / "TODO" / placeholder fields anywhere in this spec.
- ☑ Each sub-version's acceptance criterion is concrete (one column above).
- ☑ The data-flow diagram and the layer table reference identical file paths.
- ☑ Out-of-scope list is non-overlapping with the goals list.
- ☑ TDD discipline is the default, called out explicitly in §3 and §7.
