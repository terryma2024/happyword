package cool.happyword.wordmagic.core

/**
 * Parity with HarmonyOS `DevMenuRouteParams.presetEnv` passed when pushing
 * `DevMenuPage` (e.g. home version triple-tap).
 */
object DevMenuRouteParams {
    /** Focus preview deployments: refresh manifest when DevMenu opens. */
    const val PRESET_ENV_PREVIEW = "preview"
}
